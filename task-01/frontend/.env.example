# Optional: When deploying frontend (e.g. to Vercel), set this to your hosted backend URL
# Example: VITE_API_URL=https://your-backend.onrender.com
# In local development, you can leave this empty because Vite proxies /api to http://localhost:5001
VITE_API_URL=
