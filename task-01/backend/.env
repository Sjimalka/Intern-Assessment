# Server Port
PORT=5001

# PostgreSQL Connection Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pos_db

# Individual connection parameters (used if DATABASE_URL is not provided)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=pos_db
PGSSL=false
